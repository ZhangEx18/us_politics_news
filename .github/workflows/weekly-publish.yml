name: Weekly Publish
on:
  workflow_dispatch:
    inputs:
      skipped:
        description: 'news 周报已暂停'
        required: false
        default: true
        type: boolean

permissions:
  contents: write

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "news weekly publish is paused"
